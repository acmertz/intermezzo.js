addEventListener("message", (message) => {
    switch (message.type) {
        case "play":
            break;
        case "pause":
            break;
        case "seek":
            break;
    }
});